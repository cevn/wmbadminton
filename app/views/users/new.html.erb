<% provide(:title, 'Sign up') %>
<h1>Sign up</h1>

<div class="container-fluid">
  <div class="row"> 
    <p> Use this form to sign up. If you don't know who you want to be your partner, just enter 
    'Request' and we'll find one for you. </p> 
    <%= form_for(@user) do |f| %>
      <%= render 'shared/error_messages' %>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :email %>
      <%= f.text_field :email %> 

      <%= f.label :partner%>
      <%= f.text_field :partner %>

      <p> Check the boxes for the brackets for which you intend to sign up.</p> 

      <%= radio_button_tag(:md, 'MDA') %> MDA <br> 
      <%= radio_button_tag(:md, 'MDB') %> MDB <br> 
      <%= radio_button_tag(:wd, 'WDA') %> WDA <br> 
      <%= radio_button_tag(:wd, 'WDB') %> WDB <br> 
      <%= radio_button_tag(:xd, 'XDA') %> XDA <br> 
      <%= radio_button_tag(:xd, 'XDB') %> XDB <br> 

      <%= f.submit "Signup for the tournament!", class: "btn btn-large btn-primary" %>
      <%= f.submit "Clear form", class: "btn btn-large btn-danger", :type => "reset" %> 
      <% end %> 
  </div>
</div> 
